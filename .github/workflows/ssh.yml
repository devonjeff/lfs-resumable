name: SSH Access

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ssh:
    runs-on: ubuntu-latest
    steps:
      - name: Start tmate session (public SSH)
        uses: mxschmitt/action-tmate@v2
        with:
          ssh-keys: ${{ secrets.TMATE_KEY }}
          sudo: true
